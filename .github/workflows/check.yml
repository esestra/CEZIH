name: CEZIH Provjera

on:
  schedule:
    - cron: '0 8,16 * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Preuzmi kod
        uses: actions/checkout@v4

      - name: Postavi Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Instaliraj module
        run: pip install requests beautifulsoup4 pandas openpyxl supabase

      - name: Pokreni skriptu
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python cezih.py

      - name: Spremi stanje (commit)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add zadnje_stanje.txt || echo "Nema promjene"
          git commit -m "Update datuma" || echo "Nema promjene"
          git push || echo "Nema promjene"
